/*

package br.com.cahenre.encurtadorurl.adapter.out;

import org.junit.jupiter.api.Test;

import java.io.ByteArrayOutputStream;
import java.io.PrintStream;

import static org.assertj.core.api.Assertions.assertThat;

class ConsoleOutputAdapterTest {

    @Test
    void shouldPrintMessageToConsole() {
        // Arrange
        ByteArrayOutputStream outContent = new ByteArrayOutputStream();
        System.setOut(new PrintStream(outContent));
        ConsoleOutputAdapter adapter = new ConsoleOutputAdapter();

        // Act
        adapter.output("Hello World!");

        // Assert
        assertThat(outContent.toString().trim()).isEqualTo("Hello World!");
    }
}
*/
