spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/urlshort
    username: urlshort
    password: urlshort
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8080

app:
  base-url: http://localhost:8080/